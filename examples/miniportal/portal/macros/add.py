from JumpScale import j
import pdb; pdb.set_trace()
# from ..ActorsLoader import actors
from ..actors import all_actors
def add(a, b):
    # return "<h1> The sum of two numbers is {sum}</h1>".format(sum=a+b)
    return all_actors.editUser.editUser()
